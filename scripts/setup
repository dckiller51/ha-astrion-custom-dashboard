#!/usr/bin/env bash
# Installs project + dev dependencies. Run once after cloning, or whenever
# pyproject.toml's [project.optional-dependencies].dev list changes.
set -e

cd "$(dirname "$0")/.."

python3 -m pip install --upgrade pip setuptools wheel
python3 -m pip install -e ".[dev]"
pre-commit install
