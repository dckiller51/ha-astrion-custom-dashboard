{
  "config": {
    "abort": {
      "already_configured": "Device is already configured"
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "unknown": "Unexpected error"
    },
    "step": {
      "user": {
        "data": {
          "host": "Host",
          "port": "Port"
        },
        "description": "Enter the IP address of your Astrion device (the local config server on port 8080 must be reachable).",
        "title": "Set up Astrion Custom Dashboard"
      }
    }
  },
  "entity": {
    "select": {
      "activity": {
        "name": "Activity ({room})"
      },
      "page": {
        "name": "Page"
      }
    },
    "sensor": {
      "active_activity": {
        "name": "Active activity ({room})"
      },
      "current_page": {
        "name": "Current page"
      }
    }
  },
  "services": {
    "set_page": {
      "description": "Jumps the Astrion remote's dashboard to the given page.",
      "fields": {
        "page": {
          "description": "Name of the page to display, as configured in dashboard.json (case-insensitive).",
          "name": "Page"
        }
      },
      "name": "Set page"
    },
    "start_activity": {
      "description": "Starts a Astrion Activity by id, in whichever room it belongs to.",
      "fields": {
        "activity_id": {
          "description": "Id of the activity to start, as returned by the astrion device's /activities endpoint.",
          "name": "Activity ID"
        }
      },
      "name": "Start activity"
    },
    "stop_activity": {
      "description": "Stops whichever Activity is currently active in a room, without starting another. For a Harmony-backed Activity this sends PowerOff to that Activity's own hub only.",
      "fields": {
        "room": {
          "description": "Name of the room to stop, as configured in dashboard.json.",
          "name": "Room"
        }
      },
      "name": "Stop activity"
    }
  }
}
